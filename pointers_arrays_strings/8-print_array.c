#include "main.h"
#include <stdio.h>
/**
 * print_array - print n elements or array
 * @n: number array
 * Return: Always 0 (Success)
 */
void print_array(int *a, int n)
{
		/*position*/

	for (n = 0; n[a]; n++)
	
	while (n < n[a])
	{
		printf("%d, ", a[n]);
		n++;
	}
	printf("%d", a[n]);

	printf("\n");
}
